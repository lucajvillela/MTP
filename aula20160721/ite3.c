#include <stdio.h>
#include <time.h>
int main(){
    int i;
    char palavra[256];
    printf("escreva a palavra");
    scanf("%s",palavra);
    i=0;
    for(i=0;i<256;i++){
        if(i=NULL)
            i=256;
    }
        i=i++;
    printf("a palavra %s possui %d letras \n",palavra,i);

    return 0;
}

